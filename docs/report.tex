\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{longtable}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{listings}

\begin{document}
\begin{titlepage}
  \centering
  {\Huge \textbf{ CS f364 — Compiler Construction\\[1ex]}}
  {\LARGE Phase 1 Report: Lexical Analysis\\[4ex]}

  {\LARGE Project Group ID: CSf364-JULIA-Group 31\\[2ex]}

  {\LARGE Team:}\\[3ex]
  {\Large
\begin{tabular}{@{}ll@{}}
  Aryan Kamlesh Agrawal & 2023A7PS0085H (Leader) \\
  Tushya Jain & 2023A7PS0150H \\
  Gursidak Singh Bassi & 2023A7PS0033H \\
  Jainam Parekh & 2023A7PS0137H \\
\end{tabular}
}

  \vfill
  {\large Institute: BITS Pilani, Hyderabad Campus}\\
  {\large Date: \today}
\end{titlepage}

\section*{Abstract}
This report covers the CFG and lexical specification for a minimal subset of Julia,
corresponding to the Phase 1 deliverables.

\section{Context-Free Grammar (CFG)}
Non-terminals are shown in UPPERCASE:

\begin{verbatim}
Program         -> StmtList

StmtList        -> Stmt StmtList | \epsilon

Stmt            -> VarDecl | Assignment | IfStmt | WhileStmt | PrintStmt

VarDecl         -> IDENTIFIER "::" Type "=" Expr NEWLINE
                 | IDENTIFIER "=" Expr NEWLINE

Type            -> "Int64" | "Float64" | "Bool" | "String"

Assignment      -> IDENTIFIER "=" Expr NEWLINE

IfStmt          -> "if" Expr NEWLINE StmtList ElseIfList ElseBlock "end" NEWLINE
ElseIfList      -> "elseif" Expr NEWLINE StmtList ElseIfList | \epsilon
ElseBlock       -> "else" NEWLINE StmtList | \epsilon

WhileStmt       -> "while" Expr NEWLINE StmtList "end" NEWLINE

PrintStmt       -> "println" "(" Expr ")" NEWLINE

Expr            -> Expr "||" LogAndExpr | LogAndExpr
LogAndExpr      -> LogAndExpr "&&" NotExpr | NotExpr
NotExpr         -> "!" NotExpr | RelExpr
RelExpr         -> AddExpr RelOp AddExpr | AddExpr
RelOp           -> "==" | "!=" | "<" | ">" | "<=" | ">="
AddExpr         -> AddExpr AddOp MulExpr | MulExpr
AddOp           -> "+" | "-"
MulExpr         -> MulExpr MulOp UnaryExpr | UnaryExpr
MulOp           -> "*" | "/" | "%"
UnaryExpr       -> "-" UnaryExpr | Primary
Primary         -> IDENTIFIER | INT_LIT | FLOAT_LIT | STRING_LIT | BOOL_LIT | "(" Expr ")"

IDENTIFIER      -> [a-zA-Z_][a-zA-Z0-9_]*
INT_LIT         -> [0-9]+
FLOAT_LIT       -> [0-9]+\.[0-9]+
STRING_LIT      -> \"[^\"]*\"
BOOL_LIT        -> "true" | "false"
NEWLINE         -> '\\n' (or ';')
\end{verbatim}

\section{Lexical Specification (Regular Expressions)}

\begin{longtable}{p{0.3\textwidth} p{0.6\textwidth}}
Token(s) & Regular expression / literal \\
\hline
\texttt{IF} & \texttt{if} \\
\texttt{ELSEIF} & \texttt{elseif} \\
\texttt{ELSE} & \texttt{else} \\
\texttt{END} & \texttt{end} \\
\texttt{WHILE} & \texttt{while} \\
\texttt{PRINTLN} & \texttt{println} \\
\texttt{TRUE} & \texttt{true} \\
\texttt{FALSE} & \texttt{false} \\
\texttt{TYPE\_INT} & \texttt{Int64} \\
\texttt{TYPE\_FLOAT} & \texttt{Float64} \\
\texttt{TYPE\_BOOL} & \texttt{Bool} \\
\texttt{TYPE\_STR} & \texttt{String} \\
\texttt{IDENTIFIER} & \verb@[a-zA-Z_][a-zA-Z0-9_]*@ \\
\texttt{INT\_LIT} & \verb@[0-9]+@ \\
\texttt{FLOAT\_LIT} & \verb@[0-9]+\.[0-9]+@ \\
\texttt{STRING\_LIT} & \verb@"[^"]*"@ \\
\texttt{EQ, NEQ, LT, GT, LEQ, GEQ} & \texttt{==, !=, <, >, <=, >=} \\
\texttt{AND, OR, NOT} & \texttt{\&\&, ||, !} \\
\texttt{ASSIGN} & \texttt{=} \\
\texttt{DCOLON} & \texttt{::} \\
\texttt{LPAREN / RPAREN} & \texttt{( / )} \\
\texttt{SEMICOLON} & \texttt{;} \\
\texttt{NEWLINE} & \texttt{\textbackslash n} \\
\end{longtable}

\section{Notes}
- Keywords are matched before identifiers.
- Multi-character operators (==, !=, <=, >=, &&, ||, ::) are matched before
  single-character operators according to the longest-match rule.

\section{Appendix: Files Submitted}
- `phase1/CFG.md` — formal CFG
- `phase1/LexicalSpec.md` — regex per token
- `phase1/lexer.l` and `phase1/tokens.h` — flex lexer implementation
- `tests/valid_program.jl` and `tests/error_program.jl` — test inputs

\end{document}
